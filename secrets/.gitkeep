Drop a Google service-account JSON key in this folder to enable importing
from private Google Sheets, then point GOOGLE_SERVICE_ACCOUNT_KEY_FILE at
it (e.g. /run/secrets/google-service-account.json). Keys are never
committed — see .gitignore.
